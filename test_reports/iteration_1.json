{
  "summary": "First-run smoke test on NADI. Backend: 6/6 critical endpoints returned HTTP 200 with correct payloads (auth, dashboard, products, transactions, analytics, reports, insights incl. Claude Sonnet 5 AI summary ~1068 chars). Frontend: end-to-end flow login -> dashboard KPIs -> transaksi (add-to-cart works) -> riwayat -> produk -> analitik -> insight (AI summary ~1006 chars visible) -> laporan -> profil -> logout redirect all passed via data-testid selectors.",
  "tested_scope": ["frontend", "backend", "auth", "integration"],
  "passed": [
    "POST /api/auth/login returns 200 with JWT token and user profile",
    "GET /api/dashboard/summary returns today_revenue/profit/count/best_product/chart_7d/low_stock/recent_transactions",
    "GET /api/products returns 10 seeded products",
    "GET /api/transactions returns 200 transactions",
    "GET /api/analytics/overview?period=7 returns trend/hours/days/categories/top_products",
    "GET /api/reports?period=daily returns full P&L breakdown",
    "GET /api/insights returns rule-based insights + Claude Sonnet 5 AI summary (1068 chars, Bahasa Indonesia)",
    "Frontend login flow works and lands on /dashboard",
    "All 4 KPI tiles render (revenue/profit/count/best)",
    "Transaksi POS: clicking product tile adds cart-item, Riwayat tab switches",
    "Produk, Analitik, Laporan, Profil pages load with correct testids",
    "Insight page displays ai-summary element populated with Claude output",
    "Logout button redirects user to /login"
  ],
  "failed": [
    {
      "area": "frontend/analitik-charts",
      "issue": "Recharts ResponsiveContainer warns width(-1)/height(-1) on initial paint (charts still render after layout settles)",
      "evidence": "Multiple console warnings: 'The width(-1) and height(-1) of chart should be greater than 0'",
      "priority": "LOW"
    }
  ],
  "action_items": [
    "Optional: set explicit minWidth/minHeight or aspect on Recharts ResponsiveContainer parents to silence width(-1)/height(-1) warnings (cosmetic, non-blocking)"
  ],
  "success_rate": {"backend": "100%", "frontend": "100%"},
  "retest_needed": false,
  "full_testing_deferred": true,
  "test_credentials": "Verified from /app/memory/test_credentials.md - rifazaki1236@gmail.com/nadi2026 works"
}
